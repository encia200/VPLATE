<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context="com.vplate.activity.LoginMainActivity">

    <!-- 로고 이미지 -->
    <ImageView
        android:id="@+id/splash_logoImg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="82.5dp"
        android:src="@drawable/asset_1" />

    <!-- 3초 뒤 아래 애들 나오기 -->
    <ImageButton
        android:id="@+id/splash_fbLoginBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/facebook"
        android:layout_below="@id/splash_logoImg"
        android:layout_marginTop="77.5dp"
        android:layout_centerHorizontal="true"/>

    <Button
        android:id="@+id/splash_kakoLoginBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/splash_fbLoginBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/kakao"/>

    <Button
        android:id="@+id/splash_joinBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/splash_kakoLoginBtn"
        android:layout_centerHorizontal="true"
        android:background="@drawable/icon_signin"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/splash_loginBtn"
        android:layout_width="121dp"
        android:layout_height="15.5dp"
        android:layout_below="@id/splash_joinBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="14dp"
        android:background="@drawable/doyouhaveaccount"/>


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="11.5sp"
            android:textColor="#8c8c8c"
            android:gravity="center_horizontal"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="24dp"
            tools:text="계속 진행하면 VPLATE의 서비스 약관,
개인정보 보호정책에 동의한 것으로 간주됩니다."
            />

</RelativeLayout>